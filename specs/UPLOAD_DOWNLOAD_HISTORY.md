[x] Feature: Add a new stats page where the user can view their complete training history, for a selected pack, or across all packs aggregated.
[ ] Feature: The user can download their complete history, from all different packs and modes, in a text file of some sort (JSON maybe? or something else that makes sense).
[ ] Feature: The user can upload a complete history from another device. If a non-empty history exists, ask user if they want to overwrite it when they upload it.
[ ] Feature: If user uploads history and one already exists, allow user to merge the histories.
